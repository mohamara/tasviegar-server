# 🔧 راهنمای عیب‌یابی فرانت‌اند

## 🎯 **وضعیت فعلی:**

- ✅ **سرور**: در حال اجرا روی پورت 8082
- ✅ **فایل‌ها**: تمام فایل‌ها موجود هستند
- ✅ **مسیرها**: در App.tsx اضافه شده‌اند
- ❓ **مرورگر**: مشکل در باز شدن

## 🌐 **آدرس‌های تست:**

```
🏠 صفحه اصلی: http://localhost:8082/
💰 بدهی‌ها: http://localhost:8082/debts
👥 گروه‌ها: http://localhost:8082/groups
🔔 اعلان‌ها: http://localhost:8082/notifications
```

## 🔧 **مراحل عیب‌یابی:**

### 1️⃣ **بررسی سرور:**
```bash
# بررسی وضعیت سرور
lsof -i :8082

# نتیجه باید چیزی شبیه این باشد:
# node      20567 pakatchian   20u  IPv6 0xabab1c3f130f2913      0t0  TCP *:us-cli (LISTEN)
```

### 2️⃣ **تست دستی در مرورگر:**
1. **مرورگر را باز کنید**
2. **در نوار آدرس تایپ کنید**: `http://localhost:8082`
3. **Enter را فشار دهید**

### 3️⃣ **بررسی کنسول مرورگر:**
1. **F12 را فشار دهید** (Developer Tools)
2. **به تب Console بروید**
3. **خطاها را بررسی کنید**

### 4️⃣ **بررسی Network:**
1. **F12 → Network**
2. **صفحه را refresh کنید** (Ctrl+R)
3. **درخواست‌های ناموفق را بررسی کنید**

### 5️⃣ **تست با curl:**
```bash
# تست سریع
curl -I http://localhost:8082

# یا
curl http://localhost:8082 | head -10
```

## 🚨 **مشکلات احتمالی و راه‌حل:**

### **مشکل 1: صفحه خالی است**
**راه‌حل:**
1. کنسول مرورگر را بررسی کنید
2. کش مرورگر را پاک کنید (Ctrl+Shift+R)
3. سرور را restart کنید

### **مشکل 2: خطای JavaScript**
**راه‌حل:**
1. کنسول مرورگر را بررسی کنید
2. فایل‌های import را چک کنید
3. TypeScript errors را بررسی کنید

### **مشکل 3: سرور پاسخ نمی‌دهد**
**راه‌حل:**
```bash
# توقف سرور
pkill -f vite

# راه‌اندازی مجدد
npm run dev
```

### **مشکل 4: پورت در حال استفاده**
**راه‌حل:**
```bash
# بررسی پورت‌های در حال استفاده
lsof -i :8080
lsof -i :8081
lsof -i :8082

# توقف همه فرآیندها
pkill -f vite
pkill -f node
```

## 🧪 **تست‌های پیشنهادی:**

### **تست 1: صفحه اصلی**
```
http://localhost:8082/
```
**انتظار:** صفحه landing باید نمایش داده شود

### **تست 2: صفحه بدهی‌ها**
```
http://localhost:8082/debts
```
**انتظار:** لیست بدهی‌ها با 3 نمونه

### **تست 3: صفحه گروه‌ها**
```
http://localhost:8082/groups
```
**انتظار:** کارت‌های گروه‌ها با 3 نمونه

### **تست 4: صفحه اعلان‌ها**
```
http://localhost:8082/notifications
```
**انتظار:** اعلان‌ها با تب‌های مختلف

## 📋 **چک‌لیست عیب‌یابی:**

- [ ] سرور روی پورت 8082 در حال اجرا است
- [ ] فایل‌های src/main.tsx و src/App.tsx موجود هستند
- [ ] فایل‌های src/pages/Debts.tsx, Groups.tsx, Notifications.tsx موجود هستند
- [ ] مسیرها در App.tsx اضافه شده‌اند
- [ ] کنسول مرورگر خطایی نشان نمی‌دهد
- [ ] Network tab درخواست‌های ناموفق ندارد

## 🆘 **اگر هنوز مشکل دارید:**

1. **تمام مراحل بالا را انجام دهید**
2. **کنسول مرورگر را بررسی کنید**
3. **خطاهای دقیق را گزارش دهید**
4. **سرور را restart کنید**

## 🎉 **نتیجه:**

اگر تمام مراحل بالا را انجام دادید و هنوز مشکل دارید، لطفاً:
1. خطاهای کنسول را کپی کنید
2. وضعیت Network tab را گزارش دهید
3. پیام دقیق خطا را بگویید

**موفق باشید! 🚀**
